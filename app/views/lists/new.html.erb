<%= simple_form_for(@list) do |f| %>
  <%= f.input :name, placeholder: 'Enter your list name' %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>

<br>
<%= link_to 'Back to index', lists_path, class: "btn btn-primary" %>
